# Копируйте в .env и подставьте значения (docker compose подхватит автоматически).

# Локальная разработка
# HOST=127.0.0.1
# PORT=8765

# Продакшен — слушать все интерфейсы
HOST=0.0.0.0
PORT=8765

# За reverse proxy с TLS (nginx, Caddy)
HTTPS=1
TRUST_PROXY=1
